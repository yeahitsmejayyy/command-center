# command-center (v2)

Per-project task queue companion for Claude Code, rebuilt as a plugin. Under active construction — see [PLAN.md](PLAN.md) for the build plan.

**Watch the live roadmap:**

```bash
bunx serve .tracker -p 4321
```

then open http://127.0.0.1:4321 — it re-polls every 2 seconds while the agent works.

*(This README is a placeholder for the build. The real quickstart README lands in M9.)*
